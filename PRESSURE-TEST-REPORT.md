# Lottery 抽奖系统压测报告

## 1. 测试结论

本次使用 k6 对抽奖核心接口进行了 500、1000、2000 并发压测。结论如下：

- 当前本地单机环境在 500 并发下已经不能稳定满足抽奖请求，业务成功率为 86.40%，P95 响应时间约 27.72 秒。
- 1000、2000 并发下，大量请求在连接阶段被拒绝或超时，接口成功率分别下降到 28.00% 和 24.30%。
- 项目当前没有发现显式限流实现，限流覆盖范围为 0%。在高并发下系统没有进行可控降级，而是表现为连接拒绝、超时、业务失败。
- 应用进程在压测后仍然存活，数据库连接正常，说明主要问题不是应用直接崩溃，而是入口承载能力、同步抽奖链路、数据库写入链路和缺少限流保护共同导致吞吐受限。

当前版本不符合“大企业高并发抽奖系统”的生产级要求，但已经具备 DDD 分层、分库分表、策略装配、规则决策、活动领取、抽奖、发奖单等核心业务模型，适合作为进一步优化的工程基础。

## 2. 测试环境

| 项目 | 内容 |
|---|---|
| 测试时间 | 2026-09-04 |
| 测试机器 | Windows 10 本地单机 |
| Java 版本 | 1.8.0_492 |
| 应用端口 | 8081 |
| Spring Profile | local |
| 压测工具 | k6 v2.2.0 |
| 被测地址 | http://localhost:8081 |
| 被测接口 | POST /api/lottery/draw |
| 压测活动ID | 140904100353 |
| 压测策略ID | 1204100353 |
| 压测奖品 | k6压测奖品 |
| 压测奖品库存 | 5000 |
| Kafka | local 模式关闭 |
| XXL-JOB | local 模式关闭 |
| 数据库 | MySQL，lottery、lottery_01、lottery_02 |

压测后健康检查结果：

```json
{
  "success": true,
  "message": "success",
  "data": {
    "status": "UP",
    "time": "2026-09-04T10:08:32.288+08:00"
  }
}
```

压测后诊断结果显示：

- databaseConnected: true
- activities: 10
- runningActivities: 8
- runnableActivities: 7
- strategies: 9
- awards: 23
- ruleTrees: 1
- localMode: true
- kafkaProducerEnabled: false
- kafkaListenerEnabled: false
- xxlJobEnabled: false

## 3. 测试方法

使用 k6 脚本模拟用户调用抽奖接口：

```http
POST /api/lottery/draw
Content-Type: application/json

{
  "uId": "压测生成的用户ID",
  "activityId": 140904100353
}
```

每个并发用户发起一次抽奖请求，分别执行：

- 500 VU / 500 次请求
- 1000 VU / 1000 次请求
- 2000 VU / 2000 次请求

成功判断标准：

- HTTP 状态码必须为 200。
- 业务返回 code 为 `0000` 或符合可解释的业务码。
- 成功抽奖时必须返回 `awardDTO`，用于证明前端可以展示“抽中了什么奖品”。

## 4. 压测结果汇总

| 并发数 | 请求数 | HTTP 成功率 | 业务成功率 | 奖品返回率 | HTTP 失败率 | RPS | 平均响应时间 | P90 | P95 | 最大响应时间 | 结论 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 500 | 500 | 99.60% | 86.40% | 86.40% | 0.40% | 17.07/s | 16.73s | 26.51s | 27.69s | 29.26s | 不达标 |
| 1000 | 1000 | 28.00% | 27.60% | 27.60% | 72.00% | 58.95/s | 2.88s | 12.06s | 14.26s | 16.91s | 严重不达标 |
| 2000 | 2000 | 24.30% | 19.85% | 19.85% | 75.70% | 66.48/s | 9.05s | 29.90s | 29.93s | 29.95s | 严重不达标 |

说明：

- 1000、2000 并发下平均响应时间被大量“连接失败的快速请求”拉低，不能单独用平均值判断接口性能。
- 对成功建立连接的请求，1000 并发下平均响应时间约 10.31 秒，2000 并发下平均响应时间约 16.86 秒。
- 2000 并发下 P95 接近 30 秒，说明大量请求已经达到客户端超时边缘。

## 5. 限流覆盖范围

经代码搜索，未发现以下典型限流实现：

- Sentinel
- Resilience4j RateLimiter
- Guava RateLimiter
- Bucket4j
- Redis 令牌桶
- 自定义 Filter / Interceptor 限流
- 网关层限流配置

因此本项目当前限流覆盖范围为：

| 模块 | 是否有限流 | 说明 |
|---|---|---|
| 抽奖接口 | 否 | 高并发下直接打到应用和数据库链路 |
| 参与活动接口 | 否 | 没有用户级、活动级、IP 级保护 |
| 规则决策接口 | 否 | 可被高频调用 |
| 活动创建接口 | 否 | 管理类接口缺少保护 |
| 发奖接口 | 否 | 没有接口级防刷保护 |
| 全局入口 | 否 | 未发现统一限流拦截器 |

综合覆盖率：0%。

## 6. 主要问题分析

### 6.1 入口连接能力不足

1000 和 2000 并发测试中出现大量连接拒绝：

```text
connectex: No connection could be made because the target machine actively refused it.
```

这通常说明服务端入口已经无法继续接收新连接，可能与以下因素有关：

- Tomcat 最大线程数不足。
- Tomcat accept-count / max-connections 不足。
- Windows 本地单机网络栈和端口资源限制。
- 应用线程被同步业务逻辑占满，导致连接无法及时被处理。

### 6.2 抽奖链路同步操作过重

抽奖链路通常会涉及：

- 活动状态校验。
- 用户参与记录查询。
- 用户领取次数扣减。
- 策略概率计算。
- 奖品库存扣减。
- 中奖订单写入。
- 发奖单状态维护。

这些步骤如果全部同步执行，会导致接口响应时间随并发明显升高。

### 6.3 缺少可控失败机制

当前高并发下的失败表现主要是：

- HTTP 连接失败。
- 请求超时。
- 业务返回失败。

这对用户体验和系统稳定性都不理想。更合理的方式是通过限流、熔断、降级返回明确错误，例如：

```json
{
  "code": "E429",
  "info": "当前抽奖人数过多，请稍后重试"
}
```

### 6.4 本地模式未启用异步削峰

诊断结果显示：

- kafkaProducerEnabled: false
- kafkaListenerEnabled: false
- xxlJobEnabled: false

这意味着本次 local 环境没有启用 Kafka 和任务调度能力，无法通过消息队列对发奖、库存落库、订单处理等动作进行削峰。

### 6.5 日志对高并发有影响

当前应用在启动和业务执行过程中存在较多 INFO 日志。高并发场景下，同步日志输出会占用 IO 和锁资源，应降低热路径日志级别或使用异步日志。

## 7. 优化建议

### 7.1 增加限流保护

建议至少对以下接口增加限流：

| 接口 | 建议限流维度 |
|---|---|
| /api/lottery/draw | 用户ID、活动ID、IP、全局 QPS |
| /api/lottery/partake | 用户ID、活动ID |
| /api/lottery/quantification-draw | 用户ID、规则树ID |
| /api/lottery/activities | 管理端权限、IP |
| /api/lottery/distribution | 订单ID、用户ID |

推荐实现：

- 单机演示：Guava RateLimiter 或 Bucket4j。
- 分布式生产：Redis + Lua 令牌桶。
- 微服务体系：Sentinel 或网关层限流。

### 7.2 调整 Tomcat 参数

建议在本地或测试环境配置：

```yaml
server:
  tomcat:
    threads:
      max: 800
      min-spare: 100
    accept-count: 1000
    max-connections: 10000
```

注意：线程数不是越大越好，必须结合 CPU、内存、数据库连接池一起调整。

### 7.3 优化数据库连接池

建议检查并调整 Druid / Hikari 连接池：

- maxActive / maximumPoolSize
- minIdle
- maxWait
- 连接超时时间
- 慢 SQL 日志

抽奖链路应重点关注库存扣减、用户领取次数扣减、中奖订单写入这几类 SQL。

### 7.4 使用 Redis 扣减库存

生产级抽奖建议采用：

1. 活动发布时将奖品库存加载到 Redis。
2. 抽奖时使用 Redis 原子扣减库存。
3. 扣减成功后写入 MQ。
4. 消费端异步落库。
5. 定时任务进行库存和订单一致性校对。

这样可以避免每次抽奖都直接竞争数据库库存行。

### 7.5 抽奖结果异步化

当前接口可以先返回抽奖结果，发奖单生成、MQ 投递、实物发奖可以异步处理：

- 同步链路：资格校验、策略计算、库存预扣、返回中奖结果。
- 异步链路：中奖订单落库、发奖单处理、MQ 发奖、失败补偿。

### 7.6 增加幂等和防重复抽奖

建议对以下场景增加幂等控制：

- 同一用户同一活动重复点击抽奖。
- 网络重试导致重复请求。
- MQ 重复消费。
- 发奖接口重复调用。

可选方案：

- `uId + activityId + takeId` 唯一约束。
- Redis 短期幂等锁。
- 订单表唯一索引。

### 7.7 使用分布式压测环境

本次压测在 Windows 本地单机完成，压测机和被测服务在同一台机器上，会互相争抢 CPU、内存、网络和端口资源。

正式企业级压测建议：

- 单独部署被测服务。
- 单独部署 MySQL、Redis、Kafka。
- 使用多台压测机分布式压测。
- 使用 Grafana / Prometheus / Arthas / MySQL 慢 SQL 日志采集指标。

## 8. 面试展示建议

如果面试官问“这个系统是否支持高并发”，可以这样回答：

> 当前项目具备抽奖系统的核心业务模型，包括活动、策略、奖品、规则树、用户参与、中奖订单和发奖流程。原始版本在本地单机 500 并发时业务成功率约 86.40%，1000 并发后出现大量连接拒绝，说明原始实现还不是生产级高并发架构。我对它做了压测定位，发现主要瓶颈在入口线程、同步抽奖链路、数据库写入和缺少限流保护。生产化优化方向是增加 Redis 库存预扣、MQ 削峰、接口限流、幂等控制、异步发奖和连接池调优。

如果面试官问“你怎么证明系统性能瓶颈”，可以这样回答：

> 我用 k6 分别做了 500、1000、2000 并发压测。500 并发下 HTTP 成功率还接近 99.60%，但业务成功率只有 86.40%，P95 接近 27.69 秒；1000 并发后 HTTP 成功率下降到 28.00%，出现大量连接拒绝；2000 并发下 HTTP 成功率只有 24.30%。这说明瓶颈已经不只是业务逻辑慢，而是服务入口和同步处理链路都被打满了。

如果面试官问“你准备怎么优化”，可以这样回答：

> 我会分三层优化：第一层在入口增加限流和明确错误返回，避免请求无限制打进系统；第二层把奖品库存放到 Redis，用 Lua 做原子扣减，降低数据库热点行竞争；第三层把发奖单和库存落库通过 MQ 异步化，再用定时任务做补偿和一致性校验。这样可以把核心抽奖接口的同步链路压短，并且让系统在高峰期可控降级。

## 9. 附录：压测文件

| 文件 | 作用 |
|---|---|
| `.codex-loadtest/k6-draw-burst.js` | k6 抽奖接口压测脚本 |
| `.codex-loadtest/results/k6-draw-500.json` | 500 并发原始结果 |
| `.codex-loadtest/results/k6-draw-1000.json` | 1000 并发原始结果 |
| `.codex-loadtest/results/k6-draw-2000.json` | 2000 并发原始结果 |

## 10. 最终结论

当前项目在本地环境下无法支撑 1000 级以上抽奖并发，500 并发也已经出现明显业务失败和高延迟。要达到企业级高并发抽奖系统要求，需要补齐限流、Redis 库存预扣、MQ 削峰、异步发奖、幂等控制、连接池调优和分布式部署压测。
