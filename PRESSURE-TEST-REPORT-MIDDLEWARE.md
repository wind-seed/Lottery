# Lottery 抽奖系统压测报告（开启 Kafka / Nacos / XXL-Job）

## 1. 测试结论

本轮压测在本机已开启 Kafka、Nacos、ZooKeeper、XXL-Job Admin、XXL-Job 执行器的情况下进行，使用 k6 对核心抽奖接口执行 500、1000、2000 并发测试。

结论如下：

- 当前本机完整中间件模式下，500 并发已经明显超过系统稳定承载范围，HTTP 成功率只有 32.00%。
- 1000 并发 HTTP 成功率为 34.50%，2000 并发 HTTP 成功率下降到 17.25%。
- 失败主要表现为连接拒绝和请求超时，说明瓶颈已经出现在应用入口连接、Tomcat 线程处理、同步抽奖链路、数据库写入和本机资源竞争。
- Kafka 已经生效，压测后日志中可以看到 `lottery_invoice`、`lottery_activity_partake` 消息被消费；但当前抽奖接口仍包含较重同步处理，并且本机同时运行多个中间件，整体资源压力更高。
- 本次压测结果说明：项目具备 Kafka/Nacos/XXL-Job 集成能力，但当前单机本地配置不能直接支撑企业级高并发，需要进一步做限流、削峰、缓存库存、异步化和部署架构优化。

## 2. 测试环境

| 项目 | 内容 |
|---|---|
| 测试时间 | 2026-09-04 |
| 测试工具 | k6 v2.2.0 |
| 操作系统 | Windows 10 |
| Java 版本 | 1.8.0_492 |
| 应用地址 | http://localhost:8081 |
| 被测接口 | POST /api/lottery/draw |
| Nacos | 已开启，http://localhost:8848/nacos |
| ZooKeeper | 已开启，127.0.0.1:2181 |
| Kafka | 已开启，127.0.0.1:9092 |
| XXL-Job Admin | 已开启，http://localhost:7397/xxl-job-admin |
| XXL-Job 执行器 | 已开启，9998 |
| MySQL | 已连接，lottery、lottery_01、lottery_02 |
| 压测活动ID | 140904111501 |
| 压测策略ID | 1204111501 |
| 压测奖品 | Kafka开启压测奖品 |
| 压测库存 | 10000 |

压测前诊断接口确认：

```json
{
  "databaseConnected": true,
  "localMode": true,
  "kafkaProducerEnabled": true,
  "kafkaListenerEnabled": true,
  "xxlJobEnabled": true
}
```

压测后诊断接口确认：

```json
{
  "databaseConnected": true,
  "kafkaProducerEnabled": true,
  "kafkaListenerEnabled": true,
  "xxlJobEnabled": true
}
```

## 3. 测试方式

压测接口：

```http
POST /api/lottery/draw
Content-Type: application/json;charset=utf-8
```

请求体：

```json
{
  "uId": "k6自动生成的用户ID",
  "activityId": 140904111501
}
```

压测模型：

- 500 VU，500 次请求。
- 1000 VU，1000 次请求。
- 2000 VU，2000 次请求。
- 每个虚拟用户执行一次抽奖请求。
- 请求超时时间 30 秒。
- 每次请求使用不同用户ID，避免同一用户重复抽奖限制影响结果。

成功判断：

- HTTP 状态码为 200。
- 业务返回成功。
- 成功中奖时返回 `awardDTO`，用于确认前端能展示“抽中了什么奖品”。

## 4. 压测结果

| 并发数 | 请求数 | HTTP 成功 | HTTP 成功率 | HTTP 失败率 | 业务成功 | 业务成功率 | 奖品返回率 | RPS | 平均响应 | P90 | P95 | 最大响应 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 500 | 500 | 160 / 500 | 32.00% | 68.00% | 160 / 500 | 32.00% | 32.00% | 53.45/s | 1.86s | 6.82s | 7.96s | 9.33s |
| 1000 | 1000 | 345 / 1000 | 34.50% | 65.50% | 345 / 1000 | 34.50% | 34.50% | 40.59/s | 6.04s | 22.81s | 24.11s | 24.56s |
| 2000 | 2000 | 345 / 2000 | 17.25% | 82.75% | 345 / 2000 | 17.25% | 17.25% | 66.47/s | 8.75s | 29.94s | 29.96s | 29.96s |

只统计成功建立连接并拿到有效 HTTP 响应的请求：

| 并发数 | 成功响应平均耗时 | 成功响应 P95 |
|---:|---:|---:|
| 500 | 5.83s | 8.77s |
| 1000 | 17.52s | 24.46s |
| 2000 | 16.64s | 28.08s |

说明：

- 500 并发的平均响应时间看似只有 1.86 秒，是因为大量请求在连接阶段快速失败，拉低了平均值。
- 更有参考价值的是“成功响应耗时”，500 并发成功请求平均耗时已经达到 5.83 秒。
- 2000 并发下 P90、P95 接近 30 秒，说明大量请求已经达到 k6 客户端 30 秒超时边界。

## 5. Kafka 验证

本轮压测前确认 Kafka topic 已存在：

```text
lottery_activity_partake
lottery_invoice
```

压测过程中应用日志出现 Kafka 消费记录：

```text
LotteryActivityPartakeRecordListener 消费 lottery_activity_partake
LotteryInvoiceListener 消费 lottery_invoice
```

这说明 Kafka 生产者和消费者都已经工作，不是“只开了配置但没有实际使用”。

## 6. 限流覆盖范围

经代码搜索，当前项目仍未发现明确的接口限流实现，例如：

- Sentinel
- Resilience4j RateLimiter
- Guava RateLimiter
- Bucket4j
- Redis + Lua 令牌桶
- 全局 Filter / Interceptor 限流

当前限流覆盖情况：

| 模块 | 是否覆盖限流 | 说明 |
|---|---|---|
| 抽奖接口 `/api/lottery/draw` | 否 | 高并发请求直接进入应用 |
| 活动参与接口 | 否 | 没有用户级或活动级频率保护 |
| 发奖接口 | 否 | 没有防重复高频调用保护 |
| 规则决策接口 | 否 | 可被高频调用 |
| 管理类接口 | 否 | 没有管理端限流或鉴权保护 |

限流覆盖率：0%。

## 7. 问题分析

### 7.1 本机资源竞争明显

本轮测试同时运行：

- Lottery 应用
- Nacos
- ZooKeeper
- Kafka
- XXL-Job Admin
- MySQL
- k6 压测进程

这些进程都在同一台 Windows 本机上抢 CPU、内存、磁盘和网络资源。因此开启完整中间件后，单机压测成绩比关闭中间件时更差是合理现象。

### 7.2 Tomcat 入口承载不足

大量失败为：

```text
connectex: No connection could be made because the target machine actively refused it.
```

这说明请求还没有进入完整业务逻辑，就已经在连接阶段被拒绝。主要可能原因：

- Tomcat 最大连接数不足。
- accept 队列不足。
- 工作线程被业务请求占满。
- Windows 本机网络栈承载有限。

### 7.3 抽奖链路仍然偏重

即使 Kafka 已经开启，抽奖接口仍需要同步完成：

- 活动状态校验。
- 用户领取次数校验。
- 活动库存扣减。
- 策略计算。
- 奖品库存扣减。
- 中奖结果生成。
- MQ 消息发送。

这些步骤叠加后，在高并发下会形成明显的同步阻塞。

### 7.4 Kafka 开启不等于性能立刻提升

Kafka 的价值是削峰和异步解耦，但前提是系统设计要把耗时操作真正后移到异步链路。

当前系统虽然启用了 Kafka，但核心抽奖请求仍然需要同步完成较多数据库操作，所以 Kafka 只能优化部分发奖/记录处理，不能直接把接口承载能力提升到 1000 或 2000 并发。

## 8. 优化建议

### 8.1 增加入口限流

建议对抽奖接口增加限流，避免请求无限制进入系统：

- 用户级限流：同一用户每秒最多 1 次抽奖。
- 活动级限流：单活动限制 QPS。
- 全局限流：保护应用整体入口。
- 降级返回：超过阈值返回明确提示。

示例返回：

```json
{
  "code": "E429",
  "info": "当前抽奖人数过多，请稍后重试"
}
```

### 8.2 使用 Redis 原子扣减库存

生产级抽奖不建议每次直接打数据库库存。

推荐流程：

1. 活动发布时把奖品库存加载到 Redis。
2. 抽奖时通过 Redis Lua 原子扣减库存。
3. 扣减成功后返回中奖结果。
4. 中奖订单和库存流水写入 Kafka。
5. 消费端异步落库。
6. XXL-Job 定时补偿和对账。

### 8.3 缩短同步抽奖链路

同步链路只保留最核心动作：

- 参数校验。
- 活动状态校验。
- 参与资格校验。
- 策略计算。
- Redis 库存预扣。
- 返回奖品信息。

异步链路处理：

- 用户参与记录落库。
- 中奖订单落库。
- 发奖单处理。
- MQ 失败重试。
- 库存对账。

### 8.4 调整 Tomcat 和连接池

建议增加测试环境配置：

```yaml
server:
  tomcat:
    threads:
      max: 800
      min-spare: 100
    accept-count: 1000
    max-connections: 10000
```

数据库连接池也要同步调整，否则只调 Tomcat 会把压力转移到 MySQL。

### 8.5 降低热路径日志量

当前 Kafka 消费和业务流程日志较多，高并发下日志输出会影响吞吐。建议：

- 抽奖热路径只保留 WARN / ERROR。
- MQ 消费日志抽样打印。
- 使用异步日志。

## 9. 面试表达建议

可以这样向面试官说明：

> 我本地已经把 Kafka、Nacos、XXL-Job 都开启了，并重新做了 500、1000、2000 并发压测。结果显示，500 并发 HTTP 成功率只有 32%，1000 并发约 34.5%，2000 并发下降到 17.25%。失败主要是连接拒绝和请求超时。这个结果说明项目已经具备中间件集成能力，但本地单机完整中间件模式并不等同于生产集群，当前瓶颈主要在应用入口连接、同步抽奖链路、数据库写入和缺少限流保护。

如果面试官问“为什么开了 Kafka 反而更慢”，可以回答：

> Kafka 开启后只是让部分发奖和记录处理具备异步能力，但核心抽奖接口仍然有数据库校验、库存扣减、策略计算、中奖结果生成等同步步骤。同时本地机器还要运行 Kafka、ZooKeeper、Nacos、XXL-Job、MySQL 和压测工具，资源竞争更强，所以本机压测结果更差。生产环境应该把这些组件拆到独立机器或容器集群，并进一步把库存扣减、订单落库、发奖流程异步化。

如果面试官问“下一步怎么优化”，可以回答：

> 下一步我会做四件事：第一，增加接口限流和明确降级返回；第二，把奖品库存放到 Redis，用 Lua 做原子扣减；第三，把中奖订单、发奖单、库存流水写入 Kafka 异步处理；第四，用 XXL-Job 做失败补偿和库存对账。这样才能真正把抽奖接口从同步数据库链路改成高并发可承载链路。

## 10. 原始结果文件

| 文件 | 说明 |
|---|---|
| `.codex-loadtest/results/k6-draw-middleware-500.json` | 500 并发 k6 原始结果 |
| `.codex-loadtest/results/k6-draw-middleware-1000.json` | 1000 并发 k6 原始结果 |
| `.codex-loadtest/results/k6-draw-middleware-2000.json` | 2000 并发 k6 原始结果 |
| `.codex-loadtest/k6-draw-burst.js` | k6 压测脚本 |

## 11. 最终结论

开启 Kafka、Nacos、XXL-Job 后，项目可以正常启动和运行，Kafka 生产/消费也已生效。但当前本机单体部署无法支撑 500、1000、2000 级别并发抽奖。要达到企业级高并发要求，需要从架构上继续优化：入口限流、Redis 库存预扣、Kafka 削峰、异步发奖、幂等控制、连接池调优和分布式部署。
